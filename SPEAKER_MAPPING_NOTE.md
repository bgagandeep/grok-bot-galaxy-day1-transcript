# Speaker mapping note (NON-AUTHORITATIVE)

Main transcript uses `SPEAKER_XX` only.

From stream context (intros / known hosts), a *tentative* map for early segments may be:

- SPEAKER often introducing run-of-show / DX framing → likely **Matt Palmer**
- SPEAKER self-ID as Lauren / potato on Twitter → likely **Lauren (potato)**
- SPEAKER self-ID as Roshan / Rochon / product → likely **Roshan**
- Later guests (Roman, Rita, etc.) appear in later chunks — do **not** force-map without matching self-intro evidence in that segment window.

Cross-chunk speaker IDs from Gemini are **per-chunk** and may reshuffle. Global SPEAKER_XX in the merged file is a sequential remapping of discovered `spk_*` labels within each chunk, then left as-is across chunks (chunk-local consistency, not global voice ID).

Do not treat this note as confident identity for every line.
